export const metadata = {
  title: "Our Fleet | WEEGO",
  description: "Choose the perfect vehicle for your journey with WEEGO premium fleet.",
};

export default function ServicesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
