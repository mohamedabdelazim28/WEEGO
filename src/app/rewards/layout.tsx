export const metadata = {
  title: "My Rewards | WEEGO",
  description: "Manage your WEEGO loyalty points and explore premium benefits.",
};

export default function RewardsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
